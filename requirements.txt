# 核心依赖
beautifulsoup4>=4.12.0
lxml>=4.9.0
chardet==5.1.0  # 固定版本避免冲突

# 翻译相关
deep-translator>=1.11.0
translate>=3.6.1  # 替代googletrans，更稳定
googletrans==4.0.0rc1  # 如果需要，但可能还有冲突

# 替代方案：使用requests代替httpx的版本
# 移除有冲突的包，使用兼容版本

# HTTP请求
requests>=2.31.0
aiohttp>=3.9.0

# 数据处理
pandas>=2.0.0
openpyxl>=3.1.0

# 配置文件
PyYAML>=6.0
python-dotenv>=1.0.0

# 命令行工具
click>=8.1.0
rich>=13.0.0
tqdm>=4.65.0

# 压缩文件处理
py7zr>=0.20.0

# 开发工具
pytest>=7.4.0
black>=23.0.0

# 无冲突的requirements.txt（推荐）：